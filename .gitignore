# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next
out
build
dist

# misc
.DS_Store
*.pem
*.log
npm-debug.log*

# env
.env
.env*.local

# typescript
next-env.d.ts
*.tsbuildinfo

# internal product audits (unreleased designs) — kept local, not published
_audit/

# local prototypes built on the design system — kept local, not published
app/prototypes/
public/prototypes/

# note: public/r (built registry json) is intentionally committed so PMs can
# install from raw GitHub URLs without a deploy step.
